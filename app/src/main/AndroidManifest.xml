<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.LittleToothWallet"
        tools:targetApi="31">
        <activity
            android:name=".controller.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".controller.WelcomeActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".controller.RegisterActivity">
        </activity>

        <activity
            android:name=".controller.LoginActivity">
        </activity>

        <activity
            android:name=".controller.HomeActivity">
        </activity>

        <activity
            android:name=".controller.SettingsActivity">
        </activity>

        <activity
            android:name=".controller.AddExpenseEntryActivity">
        </activity>

        <activity
            android:name=".controller.AddPaymentEntryActivity">
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>